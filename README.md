Wagtail-Show-Test
=================

Simple toy test of wrapping a "show" checkbox around any Wagtail block item field.

Data Change
-----------

the database has OLD format ("<p>Hello</p>") but the code now expects NEW format ({"content": "...", "show": true}).
